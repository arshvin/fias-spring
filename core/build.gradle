dependencies {
    compile "org.springframework:spring-webmvc:$springVersion"
    compile project(":common")
    compile "javax.servlet:javax.servlet-api:3.0.1"
}

jar{
    baseName = "$jarBaseName-core"
}